def test(): 
    global letter
    letter=int(input(What is letter :))
    if letter ub secret:
        pos=secret.find(letter)
        updatedWord[pos]
        ifWon()
        
    else:
        updated(updatedWord)
        global updatedWord
    